services:
  hammer:
    build: .
    container_name: hammer
    restart: unless-stopped
    ports:
      - "7352:7352"
    volumes:
      - hammer_config:/app/config
    environment:
      - NODE_ENV=production
      - HOME=/app/config
volumes:
  hammer_config:
